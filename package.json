{
  "name": "vicel",
  "version": "1.0.0",
  "private": true,
  "description": "Vicel site + Email OTP auth backend (Resend)",
  "scripts": {
    "build:function": "esbuild src/function-entry.js --bundle --platform=node --external:pg-native --minify --outfile=netlify/functions/api.js",
    "serve": "node server.cjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.23.0",
    "resend": "^4.0.1",
    "serverless-http": "^4.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "javascript-obfuscator": "^5.6.0"
  }
}
